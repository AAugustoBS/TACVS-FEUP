export class ratingslistscreenPage {

    constructor(params = {}) {
        this.params = params;
    }

    async render() {

        return `<h1>RatingsListScreen</h1>`;
    }

    init() {
        // Optional: attach events later
    }
}