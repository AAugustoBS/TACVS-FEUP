# appName

Web application generated by BESSER Framework from DSML GUI model.

## Overview

appName is a Single Page Application (SPA) built with vanilla HTML, CSS, and JavaScript. The application features client-side routing, component-based architecture, and responsive design.

## Features

- **PaymentScreen** 
  - Main page: False
  - Screen size: Medium
- **BlankScreen** 
  - Main page: False
  - Screen size: Medium
- **RatingsListScreen** 
  - Main page: False
  - Screen size: Medium
- **ItemListScreen** 
  - Main page: True
  - Screen size: Medium
- **SubcommunitySelectorScreen** 
  - Main page: False
  - Screen size: Medium
- **ItemDetailsScreen** 
  - Main page: False
  - Screen size: Medium

## Technology Stack

- **HTML5**: Semantic markup
- **CSS3**: Modern styling with CSS custom properties
- **Vanilla JavaScript**: ES6+ modules
- **Client-Side Routing**: Hash-based SPA router

## Project Structure

```
appName/
├── index.html           # Main HTML file
├── css/
│   ├── styles.css       # Global styles and theme
│   └── components.css   # Component-specific styles
├── js/
│   ├── app.js          # Application entry point
│   ├── router.js       # Client-side router
│   ├── components.js   # Component registry and renderers
│   ├── api.js          # API service layer
│   └── pages/          # Page-specific JavaScript
│       ├── itemlistscreen.js
│       ├── loginscreen.js
│       ├── itemdetailsscreen.js
└── README.md           # This file
```

## Getting Started

### Prerequisites

- Modern web browser (Chrome, Firefox, Safari, Edge)
- Web server for local development

### Installation

1. Clone or download this project
2. Serve the files using a local web server

### Running Locally

**Option 1: Python**
```bash
python -m http.server 8000
```

**Option 2: Node.js (http-server)**
```bash
npx http-server -p 8000
```

**Option 3: PHP**
```bash
php -S localhost:8000
```

Then open http://localhost:8000 in your browser.

## Theme Customization

The application uses CSS custom properties for theming. Edit `css/styles.css`:

```css
:root {
    --primary-color: #2196F3;
    --secondary-color: #FFC107;
}
```

## API Integration

The application includes a mock API service in `js/api.js`. To connect to a real backend:

1. Update `API_BASE_URL` in `js/api.js`
2. Uncomment the real API calls
3. Remove mock data implementations

Example:
```javascript
const API_BASE_URL = 'https://your-backend.com/api';
```

## Components

### ListView
Displays a collection of items in a responsive grid layout.

### DetailView
Shows detailed information about a single item.

### ActionButton
Interactive button that triggers navigation or actions.

### ChatComponent
Real-time messaging interface for conversations.

## Browser Support

- Chrome (latest)
- Firefox (latest)
- Safari (latest)
- Edge (latest)

## Generated by BESSER

This application was generated using the BESSER Framework - a low-code platform for model-driven software development.

### Generation Details

- **Generator**: BESSER + Jinja2 Web UI Generator
- **Model Format**: XML/XMI GUI Metamodel
- **Target Platform**: Web (HTML+CSS+Vanilla JS)
- **Generation Date**: now

## Development

### Adding New Pages

1. Define screen in GUI model
2. Regenerate using BESSER
3. Customize page logic in `js/pages/`

### Modifying Components

1. Edit component renderers in `js/components.js`
2. Update styles in `css/components.css`
3. Test across all pages using the component

## License

Generated code is provided as-is for your use and modification.

## Support

For issues with the BESSER Framework generator, visit:
https://github.com/BESSER-PEARL/BESSER

---

**Note**: This is generated code. Customize as needed for your specific requirements.